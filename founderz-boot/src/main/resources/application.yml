spring:
  application:
    name: FOUNDERZ

  threads:
    virtual:
      enabled: true
  main:
    keep-alive: true

  datasource:
    driver-class-name: ${MYSQL_DRIVER}
    url: ${MYSQL_URL}
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update

server:
  port: 8080
